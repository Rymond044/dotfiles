plugin {
    hyprsplit {
        num_workspaces = 5
        # persistent_workspaces = true
    }
    hyprexpo {
        columns = 3
        gap_size = 1
        bg_col = rgb(ffffff)
        workspace_method = first 1 # [center/first] [workspace] e.g. first 1 or center m+1
    }
}
